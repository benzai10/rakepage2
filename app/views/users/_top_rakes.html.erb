<% if @top_rakes.count == 0 %>
  <div class="thumbnail" id="show-overview">
    <!-- <a href="#anchor-master-rake-overview"> -->
    <%= link_to master_rakes_path do %>
      <div class="media">
        <span class="pull-left">
          <p class="reminder-month">
            Committed
          </p>
          <p class="reminder-day">
            <i class="fa fa-bullseye"></i>
          </p>
        </span>
        <div class="media-body">
          <h5 class="media-heading">Choose a topic</h5>
          <p>
            If you commit to nothing, you’re distracted by everything.
          </p>
        </div>
      </div>
    <% end %>
    <!-- </a> -->
  </div>
  <div class="media">
    <span class="pull-left">
      <p class="reminder-month-purple">
        Updated
      </p>
      <p class="reminder-day">
        <i class="fa fa-rss"></i>
      </p>
    </span>
    <div class="media-body">
      <h5 class="media-heading">
        Customize News Feed
      </h5>
      <p>
         Choose your favorite news sources for this topic.
      </p>
    </div>
  </div>
  <div class="media">
    <span class="pull-left">
      <p class="reminder-month-purple">
        Motivated
      </p>
      <p class="reminder-day">
        <i class="fa fa-bookmark"></i>
      </p>
    </span>
    <div class="media-body">
      <h5 class="media-heading">
        Save Bookmarks
      </h5>
      <p>
         Turn bookmarks into tasks with goals, notes and reminders.
      </p>
    </div>
  </div>
  <div class="media">
    <span class="pull-left">
      <p class="reminder-month-purple">
        Productive
      </p>
      <p class="reminder-day">
        <i class="fa fa-tasks"></i>
      </p>
    </span>
    <div class="media-body">
      <h5 class="media-heading">
        Execute on Tasks
      </h5>
      <p>
         Rate and track your productivity.
      </p>
    </div>
  </div>
  <br><br><br>
<% else %>
  <div class="row">
    <% @top_rakes.each do |r| %>
      <div class="col-md-12">
        <% master_rake = MasterRake.find(r.master_rake_id) %>
        <div class="media rake-index-view" value="<%= r.id %>">
          <span class="pull-left">
            <% if master_rake.image_url.nil? || master_rake.image_url.empty? %>
              <%= image_tag("no_image.png", size: "96x54") %>
            <% else %>
              <%= image_tag(master_rake.image_url, size: "96x54") %>
            <% end %>
          </span>
          <div class="media-body">
            <h5 class="media-heading">
              <%= r.name %>
            </h5>
            <p>
              <%= render 'myrakes/top_rake_chain', toprake: r %>
            </p>
            <p class="rake-links">
              <%= link_to "Tasks", myrake_path(r, view: "tasks"), data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> Loading..." } %>
              <span class="new-rec-alert"><%= r.due_tasks_count unless r.due_tasks_count == 0 %></span>
              &nbsp;|&nbsp;
              <%= link_to "News", myrake_path(r, view: "news"), data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> Loading..." }  %>
              &nbsp;|&nbsp;
              <%= link_to "Bookmarks", myrake_path(r, view: "bookmarks"), data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> Loading..." }  %>
            </p>
          </div>
        </div>
        <br>
      </div>
    <% end %>
  </div>
<% end %>