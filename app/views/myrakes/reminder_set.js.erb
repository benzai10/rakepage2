if (<%= @reload_flag %> == 1) {
  document.location.href += "&origin=scheduled";
  // window.location.reload();
}
else {
  if ('<%= @origin %>' == 'due') {
    $('#leaflet_<%= @leaflet.id %>').remove();
    $('#anchor_leaflet_<%= @leaflet.id %>').remove();
    $('#editLeafletModal').modal('hide');
  }
}