<%= render 'dashboard/dashboard_menu' %>
<div class="container-fluid">
  <div class="row">
    <%= render 'dashboard/dashboard_nav' %>
    <div class="col-md-10">
      <h1>Users (<%= User.all.count.to_s %>)</h1>
      <table class="table">
        <thead>
          <tr>
            <th>User name</th>
            <th># of rakes</th>
            <th>Last sign in</th>
          <tr>
        <thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= link_to user.username, dashboard_user_path(user) %></td>
              <td><%= user.myrakes.count.to_s %>
              <td><%= user.last_sign_in_at %>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @users %>
    </div>
  </div>
</div>